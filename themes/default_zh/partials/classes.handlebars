<h1>{{name}} 类</h1>
<div class="box meta">
    {{#if uses}}
        <div class="uses">
            使用
            <ul class="inline commas">
                {{#uses}}
                    <li><a href="{{.}}.html">{{.}}</a></li>
                {{/uses}}
            </ul>
        </div>
    {{/if}}

    {{#if extends}}
        <div class="extends">
            继承 {{#crossLink extends}}{{/crossLink}}
        </div>
    {{/if}}

    {{#if foundAt}}
        <div class="foundat">
            于<a href="{{foundAt}}">`{{{file}}}:{{{line}}}`</a>定义
        </div>
    {{/if}}

    {{#if module}}
        {{#if submodule}}
            模组: {{#crossLinkModule submodule}}{{/crossLinkModule}}<br>
            父模组: {{#crossLinkModule module}}{{/crossLinkModule}}
        {{else}}
            模组: {{#crossLinkModule module}}{{/crossLinkModule}}
        {{/if}}
    {{/if}}

    {{#if since}}
        <p>Available since {{since}}</p>
    {{/if}}
</div>

{{#if deprecated}}
    <div class="box deprecated">
        <p>
        {{#if deprecationMessage}}
            <strong>Deprecated:</strong> {{deprecationMessage}}
        {{else}}
            该类已弃用
        {{/if}}
        </p>
    </div>
{{/if}}

<div class="box intro">
    {{{classDescription}}}
</div>

<!-- Class member index -->

<a name="index" class="anchor-link"></a>
<div class="index">
    <h2>索引</h2>
    {{>options}}

{{#if properties}}
    <div class="index-section properties">
        <h3>属性(Property)</h3>
        <ul class="index-list properties">
            {{#properties}}
                <li class="index-item property{{#if access}} {{access}}{{/if}}{{#if deprecated}} deprecated{{/if}}{{#if overwritten_from}} inherited{{/if}}">
                    <a href="#property_{{name}}">{{name}}</a>
                    {{#if static}}
                        <span class="flag static">静态</span>
                    {{/if}}
                </li>
            {{/properties}}
        </ul>
    </div>
{{/if}}

{{#if attrs}}
    <div class="index-section attrs">
        <h3>属性(Attribute)</h3>

        <ul class="index-list attrs">
            {{#attrs}}
                <li class="index-item attr{{#if access}} {{access}}{{/if}}{{#if deprecated}} deprecated{{/if}}{{#if overwritten_from}} inherited{{/if}}">
                    <a href="#attr_{{name}}">{{name}}</a>
                </li>
            {{/attrs}}
        </ul>
    </div>
{{/if}}

{{#if methods}}
    <div class="index-section methods">
        <h3>方法</h3>

        <ul class="index-list methods">

            {{#is_constructor}}
                <li class="index-item method{{#if access}} {{access}}{{/if}}{{#if deprecated}} deprecated{{/if}} constructor">
                    <a href="#method_{{name}}">{{name}}</a>
                    <span class="flag constructor">constructor</span>
                    {{#if static}}
                        <span class="flag static">静态</span>
                    {{/if}}
                    {{#if deprecated}}
                        <span class="flag deprecated">已废弃</span>
                    {{/if}}
                </li>
            {{/is_constructor}}

            {{#methods}}
                <li class="index-item method{{#if access}} {{access}}{{/if}}{{#if deprecated}} deprecated{{/if}}{{#if overwritten_from}} inherited{{/if}}">
                    <a href="#method_{{name}}">{{name}}</a>
                    {{#if static}}
                        <span class="flag static">静态</span>
                    {{/if}}
                    {{#if deprecated}}
                        <span class="flag deprecated">已废弃</span>
                    {{/if}}
                </li>
            {{/methods}}
        </ul>
    </div>
{{/if}}

{{#if events}}
    <div class="index-section events">
        <h3>事件</h3>

        <ul class="index-list events">
            {{#events}}
                <li class="index-item event{{#if access}} {{access}}{{/if}}{{#if deprecated}} deprecated{{/if}}{{#if overwritten_from}} inherited{{/if}}{{#if extended_from}} inherited{{/if}}">
                    <a href="#event_{{name}}">{{name}}</a>

                    {{#if static}}
                        <span class="flag static">静态</span>
                    {{/if}}
                    {{#if deprecated}}
                        <span class="flag deprecated">已废弃</span>
                    {{/if}}
                </li>
            {{/events}}
        </ul>
    </div>
{{/if}}

{{#if extends}}
    <div class="index-section inherits-members">
        <h3>继承成员变量</h3>
        
        <h4>属性(Property)</h4>
        <ul class="index-list properties">
        {{#inheritedItems.properties}}
            <li class="index-item property">
                <a href="{{extended_from}}.html#property_{{name}}">{{name}}</a>
            </li>
        {{/inheritedItems.properties}}
        </ul>
        
        <h4>属性(Attribute)</h4>
        <ul class="index-list attrs">
        {{#inheritedItems.attrs}}
            <li class="index-item attr">
                <a href="{{extended_from}}.html#attr_{{name}}">{{name}}</a>
            </li>
        {{/inheritedItems.attrs}}
        </ul>

        <h4>方法</h4>
        <ul class="index-list methods">
        {{#inheritedItems.methods}}
            <li class="index-item method">
                <a href="{{extended_from}}.html#method_{{name}}">{{name}}</a>
            </li>
        {{/inheritedItems.methods}}
        </ul>

        <h4>事件</h4>
        <ul class="index-list events">
        {{#inheritedItems.events}}
            <li class="index-item event">
                <a href="{{extended_from}}.html#event_{{name}}">{{name}}</a>
            </li>
        {{/inheritedItems.events}}
        </ul>

    </div>
{{/if}}

{{#if children}}
    <div class="index-section inherits-tree">
        <h3>继承树</h3>
        {{> class-tree}}
    </div>
{{/if}}

</div>

<div class="class-detail">

{{#if properties}}
    <div class="properties-detail">
        <h2>属性(Property)</h2>
        {{#properties}}
            {{>props}}
        {{/properties}}
        <div class="no-visible-items-message">
            <p>There are no properties that match your current filter settings. You can change your filter settings in the index section on this page. <a href="#index" class="index-jump-link">index</a></p>
        </div>
    </div>
{{/if}}

{{#if attrs}}
    <div class="attrs-detail">
        <h2>属性(Attribute)</h2>
        {{#attrs}}{{>attrs}}{{/attrs}}
    </div>
{{/if}}

{{#is_constructor}}
    <div class="constructor">
        <h2>构造函数</h2>{{>method}}
    </div>
{{/is_constructor}}

{{#if methods}}
    <div class="methods-detail">
        <h2>方法</h2>
        {{#methods}}{{>method}}{{/methods}}
        <div class="no-visible-items-message">
            <p>There are no methods that match your current filter settings. You can change your filter settings in the index section on this page. <a href="#index" class="index-jump-link">index</a></p>
        </div>
    </div>
{{/if}}

{{#if events}}
    <div class="events-detail">
        <h2>事件</h2>
        {{#events}}{{>events}}{{/events}}
        <div class="no-visible-items-message">
            <p>There are no events that match your current filter settings. You can change your filter settings in the index section on this page. <a href="#index" class="index-jump-link">index</a></p>
        </div>
    </div>
{{/if}}

</div>

