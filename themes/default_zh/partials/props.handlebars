<a name="property_{{name}}" class="anchor-link"></a>
<div class="property item{{#if access}} {{access}}{{/if}}{{#if deprecated}} deprecated{{/if}}{{#if extended_from}} inherited{{/if}}">
    <h3 class="name"><code>{{name}}</code>
    <span class="type">{{#crossLink type}}{{/crossLink}}</span>

    {{#if deprecated}}
        <span class="flag deprecated"{{#if deprecationMessage}} title="{{deprecationMessage}}"{{/if}}>已弃用</span>
    {{/if}}

    {{#if access}}
        <span class="flag {{access}}">{{access}}</span>
    {{/if}}

    {{#if final}}
        <span class="flag final">final</span>
    {{/if}}

    {{#if static}}
        <span class="flag static">静态</span>
    {{/if}}

    </h3>

    <div class="meta">
        {{#if overwritten_from}}
            <p>继承自
            <a href="{{crossLinkRaw overwritten_from/class}}#property_{{overwritten_from/name}}" class="type">
                {{overwritten_from/class}}
            </a>
            {{#if foundAt}}
            并重写于
            {{/if}}
        {{else}}
            {{#if extended_from}}
                <p>继承自
                <a href="{{crossLinkRaw extended_from}}#property_{{name}}" class="type">{{extended_from}}</a>:
            {{else}}
                {{#providedBy}}
                    <p>Provided by the <a href="../modules/{{.}}.html" class="module-name">{{.}}</a> module.</p>
                {{/providedBy}}
                <p>
                {{#if foundAt}}
                定义于
                {{/if}}
            {{/if}}
        {{/if}}
        {{#if foundAt}}
        <a href="{{foundAt}}">`{{{file}}}:{{{line}}}`</a>
        {{/if}}
        </p>

        {{#if deprecationMessage}}
            <p class="deprecated">Deprecated: <span class="message">{{deprecationMessage}}</span></p>
        {{/if}}

        {{#if since}}
            <p class="since">于<span class="message">{{since}}</span>之后可用</p>
        {{/if}}
    </div>

    <div class="extended-detail">
        <div class="description">
            {{{propertyDescription}}}
        </div>

        {{#if default}}
            <p class="default"><strong>默认值:</strong> <span class="default-value">{{default}}</span></p>
        {{/if}}

        {{#example}}
            <div class="example">
                <h4>例子:</h4>

                <div class="example-content">
                    {{{.}}}
                </div>
            </div>
        {{/example}}

        {{#if subprops}}
            <h4>子属性:</h4>

            <ul class="params-list">
                {{#subprops}}
                <li class="param">
                    {{#if optional}}
                        <code class="param-name optional">[{{name}}{{#if optdefault}}={{optdefault}}{{/if}}]</code>
                        <span class="type">{{#crossLink type}}{{/crossLink}}</span>
                        <span class="flag optional" title="This property is optional.">optional</span>
                    {{else}}
                        <code class="param-name">{{name}}</code>
                        <span class="type">{{#crossLink type}}{{/crossLink}}</span>
                    {{/if}}

                    <div class="param-description">
                        {{{description}}}
                    </div>

                    {{#if subprops}}
                        <ul class="params-list">
                            {{#subprops}}
                            <li class="param">
                                {{#if optional}}
                                    <code class="param-name optional">[{{name}}{{#if optdefault}}={{optdefault}}{{/if}}]</code>
                                    <span class="type">{{#crossLink type}}{{/crossLink}}</span>
                                    <span class="flag optional" title="This property is optional.">选填</span>
                                {{else}}
                                    <code class="param-name">{{name}}</code>
                                    <span class="type">{{#crossLink type}}{{/crossLink}}</span>
                                {{/if}}

                                <div class="param-description">
                                    {{{description}}}
                                </div>
                            </li>
                            {{/subprops}}
                        </ul>
                    {{/if}}
                </li>
                {{/subprops}}
            </ul>
        {{/if}}
    </div>
</div>
